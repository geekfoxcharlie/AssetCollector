# 这里不是流程目录

使用方流程存放在用户数据目录，不进本仓库：

`--skills-dir` > `ASSETCOLLECTOR_SKILLS_DIR` > `$XDG_DATA_HOME/assetcollector` > `~/.local/share/assetcollector`

布局：`<catalog>/<namespace>/<skill>/SKILL.md`。通用流程用 `shared`，项目流程与 AssetHub 使用同一命名空间 id。
